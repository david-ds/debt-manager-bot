version: '2'
services:
  web:
    image: node:5
    ports:
      - "3000:3000"
    volumes:
      - ./:/usr/src/app
    depends_on:
      - mongo
    working_dir: /usr/src/app
  mongo:
    image: mongo:3
    volumes:
      - /data/db
